_const logger = require('../../utils/logger');

// La funcionalidad de Veo API ha sido omitida por razones de viabilidad y políticas de uso ético.
// Este archivo sirve como placeholder para mantener la estructura del proyecto.

async function generateShortVideo(prompt, aspectRatio = "9:16") {
  logger.warn(`Llamada a la función generateShortVideo. La funcionalidad de Veo API no está implementada.`);
  return {
    status: 'omitted',
    message: 'Funcionalidad de generación de video omitida por no ser viable/ética.',
    prompt: prompt
  };
}

module.exports = {
  generateShortVideo
};_
